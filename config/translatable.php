<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 後台多語系設定
    |--------------------------------------------------------------------------
    |
    | 定義後台表單要支援的語系。所有使用 TranslatableInput / TranslatableEditor
    | 的元件都會根據這個設定自動產生對應語言的欄位。
    |
    | 格式：'語系代碼' => ['label' => '顯示名稱', 'placeholder' => '佔位文字前綴']
    |
    | 若只需要中文，只保留 zh_TW 即可；若需新增語言，加一行就會生效。
    |
    */

    'locales' => [
        'zh_TW' => [
            'label'       => '中文',
            'placeholder' => '請輸入中文',
        ],
       
    ],

    /*
    |--------------------------------------------------------------------------
    | 主要語系
    |--------------------------------------------------------------------------
    |
    | 標記為主語系的欄位會加上必填 (*) 標示
    |
    */

    'primary' => 'zh_TW',

];
